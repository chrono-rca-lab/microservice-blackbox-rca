#!/usr/bin/env bash
# TODO: add network latency inside $TARGET pod for $DURATION seconds.
# Expects: TARGET, DURATION, NAMESPACE, DELAY_MS environment variables.
echo "[net_delay] not yet implemented"
